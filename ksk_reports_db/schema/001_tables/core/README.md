# Система отчётов КСК — Архитектура БД

**Версия:** 1.0  
**Дата:** 2025-10-27  
**Схема:** `upoa_ksk_reports`  
**СУБД:** PostgreSQL

---

## 📋 Содержание

1. [Обзор системы](#обзор-системы)
2. [Иерархия таблиц](#иерархия-таблиц)
3. [Описание таблиц](#описание-таблиц)
4. [Индексы и оптимизация](#индексы-и-оптимизация)
5. [Служебные объекты](#служебные-объекты)

---

## Обзор системы

Система отчётов КСК предназначена для хранения и анализа результатов санкционных проверок платежей. Система обрабатывает **~3M записей в день** и требует оптимизации для работы с HDD и большими JSONB данными.

### Ключевые характеристики

- **Объём данных:** 3TB+ JSONB (~3M записей/день)
- **Хранилище:** HDD (требуется оптимизация EXTERNAL STORAGE)
- **Архитектура:** Иерархическая (результат → фигуранты → совпадения)
- **Партиционирование:** RANGE по timestamp (ежедневно)

---

## Иерархия таблиц

```
ksk_result (основная)
│
├─ 1:N связь (source_id, output_timestamp)
│
└─→ ksk_figurant (фигуранты)
   │
   ├─ 1:N связь (figurant_id, timestamp)
   │
   └─→ ksk_figurant_match (совпадения алгоритмов)

+ ksk_system_operations_log (системный лог, независимая таблица)
```

### Связи между таблицами

| Родитель | Потомок | Тип | Особенность |
|----------|---------|-----|-----------|
| `ksk_result` | `ksk_figurant` | N:1 (source_id) | CASCADE DELETE |
| `ksk_figurant` | `ksk_figurant_match` | N:1 (figurant_id) | CASCADE DELETE |

**Внимание:** CASCADE DELETE может привести к массовому удалению при ошибке. Проверяйте удаления перед выполнением!

---

## Описание таблиц

### 1. ksk_result

**Назначение:** Основная таблица результатов проверки платежей

**Характеристики:**
- **Объём:** ~3M записей/день
- **Партиционирование:** RANGE по `output_timestamp` (ежедневно)
- **PRIMARY KEY:** `(id, output_timestamp)`
- **Партиция по умолчанию:** `part_ksk_result_default`

**Колонки:**

| Колонка | Тип | Особенность | Назначение |
|---------|-----|-----------|-----------|
| `id` | INTEGER GENERATED AS IDENTITY | PRIMARY KEY | Уникальный ID |
| `date` | DATE | NOT NULL | Дата записи |
| `corr_id` | VARCHAR(100) | NOT NULL + индекс | Корреляционный ID платежа |
| `input_timestamp` | TIMESTAMP(3) | | Время входящего запроса |
| `output_timestamp` | TIMESTAMP(3) | NOT NULL + PARTITION KEY | Время результата (ключ партиции) |
| `input_json` | JSONB | EXTERNAL STORAGE | Входящий JSON запрос |
| `output_json` | JSONB | EXTERNAL STORAGE | Выходящий JSON результат |
| `payment_type` | VARCHAR(20) | NOT NULL + индекс | Тип платежа (i_*, o_*, t_*, m_*, v_*) |
| `resolution` | VARCHAR(20) | NOT NULL | Резолюция (ALLOW, BLOCK, REVIEW) |
| `list_codes` | TEXT[] | + GIN индекс | Массив кодов санкционных списков |
| `has_bypass` | VARCHAR(10) | DEFAULT 'empty' | Признак обхода (empty/yes/no) |
| Денормализованные поля | TEXT | | Поля платежей (payer_*, receiver_*, amount, currency) |

**JSONB хранилище:**
- `input_json` и `output_json` используют `STORAGE EXTERNAL`
- Критично для HDD: экономит место в буфере (~50-70% экономия)
- Немного медленнее, чем MAIN, но для HDD это оправдано

---

### 2. ksk_figurant

**Назначение:** Фигуранты (лица/организации, попадающие в проверку)

**Характеристики:**
- **Связь:** N:1 с `ksk_result` через `source_id`
- **Партиционирование:** RANGE по `timestamp` (ежедневно)
- **PRIMARY KEY:** `(id, timestamp)`
- **Партиция по умолчанию:** `part_ksk_figurant_default`
- **FOREIGN KEY:** `(source_id, timestamp)` → `ksk_result(id, output_timestamp)` ON DELETE CASCADE

**Колонки:**

| Колонка | Тип | Особенность | Назначение |
|---------|-----|-----------|-----------|
| `id` | INTEGER GENERATED AS IDENTITY | PRIMARY KEY | Уникальный ID |
| `source_id` | INTEGER | NOT NULL + FK + индекс | Ссылка на ksk_result.id |
| `date` | DATE | NOT NULL | Дата записи |
| `timestamp` | TIMESTAMP(3) | NOT NULL + PARTITION KEY | Время (ключ партиции) |
| `figurant` | JSONB | NOT NULL + EXTERNAL STORAGE | Полная информация о фигуранте |
| `figurant_index` | INTEGER | NOT NULL | Порядковый номер в результате |
| `resolution` | VARCHAR(20) | NOT NULL | Резолюция для фигуранта |
| `is_bypass` | VARCHAR(10) | DEFAULT 'no' + partial индекс | Обход (yes/no) |
| `list_code` | TEXT | + partial индекс | Код санкционного списка |
| `name_figurant` | TEXT | | Имя из санкционного списка |
| `president_group` | TEXT | | Группа президента (если есть) |
| `auto_login` | BOOLEAN | | Автологин (признак типа фигуранта) |
| `has_exclusion` | BOOLEAN | | Имеет исключение |
| `exclusion_phrase` | TEXT | | Фраза исключения |
| `exclusion_name_list` | TEXT | | Список имён исключения |

---

### 3. ksk_figurant_match

**Назначение:** Совпадения алгоритмов поиска для каждого фигуранта

**Характеристики:**
- **Связь:** N:1 с `ksk_figurant` через `figurant_id`
- **Партиционирование:** RANGE по `timestamp` (ежедневно)
- **PRIMARY KEY:** `(id, timestamp)`
- **Партиция по умолчанию:** `part_ksk_figurant_match_default`
- **FOREIGN KEY:** `(figurant_id, timestamp)` → `ksk_figurant(id, timestamp)` ON DELETE CASCADE

**Колонки:**

| Колонка | Тип | Особенность | Назначение |
|---------|-----|-----------|-----------|
| `id` | INTEGER GENERATED AS IDENTITY | PRIMARY KEY | Уникальный ID |
| `figurant_id` | INTEGER | NOT NULL + FK + индекс | Ссылка на ksk_figurant.id |
| `date` | DATE | NOT NULL | Дата записи |
| `timestamp` | TIMESTAMP(3) | NOT NULL + PARTITION KEY | Время (ключ партиции) |
| `match` | JSONB | NOT NULL + EXTERNAL STORAGE | Полная информация о совпадении |
| `match_index` | INTEGER | NOT NULL | Порядковый номер совпадения |
| `algorithm` | VARCHAR(100) | NOT NULL + индекс | Название алгоритма поиска |
| `match_value` | TEXT | | Значение совпадения |
| `match_payment_field` | TEXT | | Поле платежа с совпадением |
| `match_payment_value` | TEXT | | Значение поля платежа |

---

### 4. ksk_system_operations_log

**Назначение:** Системный лог операций (создание партиций, запуск отчётов и т.д.)

**Характеристики:**
- **БЕЗ партиционирования** (это системный лог, редкие записи)
- **PRIMARY KEY:** `id` (простой)
- **CHECK constraint:** `status IN ('success', 'error')`

**Колонки:**

| Колонка | Тип | Особенность | Назначение |
|---------|-----|-----------|-----------|
| `id` | INTEGER GENERATED AS IDENTITY | PRIMARY KEY | Уникальный ID |
| `operation_code` | VARCHAR(50) | NOT NULL + индекс | Код операции |
| `operation_name` | VARCHAR(200) | NOT NULL | Название операции |
| `begin_time` | TIMESTAMP | DEFAULT NOW() + индекс | Начало операции |
| `end_time` | TIMESTAMP | | Конец операции |
| `duration` | INTERVAL | | Длительность (end_time - begin_time) |
| `status` | VARCHAR(20) | NOT NULL + CHECK + индекс | Статус (success/error) |
| `info` | TEXT | | Доп. информация |
| `err_msg` | TEXT | | Сообщение об ошибке |

---

## Индексы и оптимизация

### ksk_result — 5 индексов

| Имя | Тип | Колонка(и) | Применение | Примечание |
|-----|-----|-----------|-----------|-----------|
| `idx_ksk_result_corr_id` | B-tree | `corr_id` | Поиск по ID платежа (поддержка) | Для отладки |
| `idx_ksk_result_output_ts_brin` | BRIN | `output_timestamp` | Временные фильтры (WHERE output_timestamp > ...) | ~1000x меньше B-tree |
| `idx_ksk_result_aggregation` | B-tree + INCLUDE | `(output_timestamp, resolution, has_bypass)` INCLUDE `payment_type` | Агрегации в отчётах | Index-only scan, без доступа к таблице |
| `idx_ksk_result_list_codes_gin` | GIN | `list_codes` | Поиск по массиву (WHERE list_codes && ARRAY[...]) | 10-20x ускорение |
| `idx_ksk_result_payment_type` | B-tree | `payment_type` | Фильтрация по типам платежей | В большинстве отчётов |

**Статистика BRIN:**
- BRIN на 1000x компактнее B-tree для временных рядов
- Идеален для партиционированных таблиц с упорядоченными данными
- На HDD критичен: экономит место на диске и в буфере

---

### ksk_figurant — 5 индексов

| Имя | Тип | Колонка(и) | Применение | Примечание |
|-----|-----|-----------|-----------|-----------|
| `idx_ksk_figurant_ts_brin` | BRIN | `timestamp` | Временные фильтры (WHERE timestamp > ...) | ~1000x меньше B-tree |
| `idx_ksk_figurant_list_code` | B-tree (partial) | `list_code` WHERE `list_code IS NOT NULL` | Поиск по санкционному списку | Только NOT NULL значения (экономия) |
| `idx_ksk_figurant_source_id` | B-tree | `source_id` | JOIN с ksk_result | Критичен для связей |
| `idx_ksk_figurant_resolution` | B-tree | `resolution` | Агрегация по резолюции | Фильтрация результатов |
| `idx_ksk_figurant_is_bypass_yes` | B-tree (partial) | `is_bypass` WHERE `is_bypass = 'yes'` | Поиск обходов проверки | Высокая селективность (редкие случаи) |

**Partial индексы:**
- `list_code`: Экономит место, пропускает NULL
- `is_bypass='yes'`: Только 'yes' случаи (~1-5% от данных), игнорирует 'no' и 'empty'

---

### ksk_figurant_match — 3 индекса

| Имя | Тип | Колонка(и) | Применение | Примечание |
|-----|-----|-----------|-----------|-----------|
| `idx_ksk_match_ts_brin` | BRIN | `timestamp` | Временные фильтры (WHERE timestamp > ...) | ~1000x меньше B-tree |
| `idx_ksk_match_figurant_id` | B-tree | `figurant_id` | JOIN с ksk_figurant | Критичен для связей |
| `idx_ksk_match_algorithm` | B-tree | `algorithm` | Аналитика по алгоритмам поиска | SELECT algorithm, COUNT(*) FROM ... GROUP BY algorithm |

---

### ksk_system_operations_log — 3 индекса

| Имя | Тип | Колонка(и) | Применение | Примечание |
|-----|-----|-----------|-----------|-----------|
| `idx_ksk_system_operations_log_operation_code` | B-tree | `operation_code` | Поиск истории операции | WHERE operation_code = 'create_partitions' |
| `idx_ksk_system_operations_log_begin_time` | B-tree | `begin_time` | Временная фильтрация | WHERE begin_time > now() - interval '1 day' |
| `idx_ksk_system_operations_log_status` | B-tree | `status` | Поиск ошибок | WHERE status = 'error' |

---

## Служебные объекты

### Функции

#### `jsonb_object_length(input_data jsonb) → integer`

Универсальная функция для определения "размера" JSONB значения.

**Логика:**
- Объект `{"a":1,"b":2}` → количество полей (2)
- Массив `[1,2,3]` → количество элементов (3)
- Скаляр `"text"` или `42` → 1
- `null` → 0

**Применение:** Валидация структуры JSONB данных в отчётах

---

#### `add_column_if_not_exists(p_table_name text, p_column_name text, p_column_type text, p_column_default text = NULL) → void`

Идемпотентная функция для добавления колонок к таблице.

**Особенности:**
- Работает для всех типов данных PostgreSQL
- Поддерживает DEFAULT выражения
- Полностью идемпотентна (можно запускать многократно)
- Выводит NOTICE сообщения о статусе

**Применение:** Миграции и расширение таблиц без пересоздания

---

### Типовые запросы

#### Поиск платежа по ID

```sql
SELECT * FROM ksk_result 
WHERE corr_id = 'abc-123-def'
AND output_timestamp > now() - interval '7 days'
ORDER BY output_timestamp DESC;
```

**Индексы:** `idx_ksk_result_corr_id`, `idx_ksk_result_output_ts_brin`

---

#### Агрегация по резолюциям

```sql
SELECT resolution, COUNT(*) as cnt
FROM ksk_result
WHERE output_timestamp >= current_date
AND output_timestamp < current_date + interval '1 day'
GROUP BY resolution;
```

**Индексы:** `idx_ksk_result_aggregation` (index-only scan)

---

#### Поиск по санкционным спискам

```sql
SELECT id, corr_id, list_codes
FROM ksk_result
WHERE list_codes && ARRAY['SDN', 'OFAC']
AND output_timestamp > now() - interval '30 days';
```

**Индексы:** `idx_ksk_result_list_codes_gin`, `idx_ksk_result_output_ts_brin`

---

#### Мониторинг операций

```sql
SELECT operation_code, status, COUNT(*) as cnt
FROM ksk_system_operations_log
WHERE begin_time > now() - interval '24 hours'
GROUP BY operation_code, status;
```

**Индексы:** `idx_ksk_system_operations_log_begin_time`, `idx_ksk_system_operations_log_status`

---

## Оптимизация для HDD

### STORAGE EXTERNAL

Все JSONB колонки используют `STORAGE EXTERNAL`:

```sql
ALTER TABLE ksk_result 
  ALTER COLUMN input_json SET STORAGE EXTERNAL,
  ALTER COLUMN output_json SET STORAGE EXTERNAL;
```

**Преимущества:**
- Не сжимаются в памяти (хранятся как есть)
- На HDD: экономия 50-70% в буфере
- Доступ в основной памяти: немного медленнее, но для HDD это оптимально

---

### BRIN индексы

Используются вместо B-tree для временных колонок:

- **BRIN:** ~1MB на 100GB данных
- **B-tree:** ~1GB на 100GB данных (1000x больше)

На HDD BRIN критичен: экономит место на диске и позволяет полностью поместить индекс в кэш.

---

### Partial индексы

Используются для низкоселективных колонок:

```sql
-- Только NOT NULL значения
CREATE INDEX idx_ksk_figurant_list_code 
  ON ksk_figurant (list_code) 
  WHERE list_code IS NOT NULL;

-- Только 'yes' значения (~1-5% данных)
CREATE INDEX idx_ksk_figurant_is_bypass_yes 
  ON ksk_figurant (is_bypass) 
  WHERE is_bypass = 'yes';
```

**Экономия:** 70-90% размера индекса

---

## Партиционирование

### RANGE по timestamp

Все таблицы (кроме `ksk_system_operations_log`) партиционированы:

```sql
PARTITION BY RANGE (output_timestamp)  -- или timestamp
```

**Конфигурация:**
- Тип: RANGE (по дате)
- Уровень детализации: Ежедневно (рекомендуется)
- Дефолтная партиция: `part_*_default` (для непредвиденных значений)

**Преимущества:**
- Constraint exclusion: пропускает полные партиции при фильтрации
- Параллельное сканирование нескольких партиций
- Эффективная очистка старых данных (DROP PARTITION)
- На HDD: лучшая локальность доступа

### Рекомендации

1. **Создание партиций:** Заранее (за 1-2 дня до необходимости)
2. **Удаление партиций:** По расписанию (хранить 90-180 дней)
3. **Мониторинг:** Проверяйте `constraint_exclusion = on`

---

## Конфигурация PostgreSQL

### Рекомендуемые параметры

```sql
-- Оптимизация для HDD и 3TB JSONB

-- Уровень параллелизма
SET max_parallel_workers_per_gather = 4;
SET max_parallel_workers = 4;

-- Работа с памятью (для 8GB RAM)
SET work_mem = '256MB';         -- на один worker
SET maintenance_work_mem = '1GB';

-- Оптимизация партиций
SET constraint_exclusion = on;  -- КРИТИЧНО! Пропускает полные партиции

-- BRIN индексы
SET random_page_cost = 4;       -- Для HDD (по умолчанию 4)

-- JSON
SET jit = off;                  -- JIT может быть медленнее на HDD
```

---

## История изменений

| Дата | Версия | Изменения |
|------|--------|----------|
| 2025-10-27 | 1.0 | Первая версия. Идемпотентные скрипты для всех таблиц, функции и индексы |

---

## Контакты

**Поддержка:** Database Engineering Team  
**Вопросы по оптимизации:** Обратитесь к специалисту по PostgreSQL

---

## Приложение: Миграция и развёртывание

### Порядок создания таблиц

1. **ksk_system_operations_log** (независимая, нет FK)
2. **ksk_result** (основная)
3. **ksk_figurant** (зависит от ksk_result)
4. **ksk_figurant_match** (зависит от ksk_figurant)

### Идемпотентность

Все скрипты используют:
- `IF NOT EXISTS` для таблиц
- `add_column_if_not_exists()` для колонок
- `CREATE INDEX IF NOT EXISTS` для индексов
- Динамическое удаление неиспользуемых индексов

**Результат:** Скрипты безопасны для повторного запуска в PROD.

---

**Документация актуальна на:** 2025-10-27
