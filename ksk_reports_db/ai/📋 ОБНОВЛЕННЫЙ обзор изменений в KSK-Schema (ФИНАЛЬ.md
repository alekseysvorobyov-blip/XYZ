
## 📋 ОБНОВЛЕННЫЙ обзор изменений в KSK-Schema (ФИНАЛЬНЫЙ)

Требуется обновить **KSK-Schema-v3.8.1-GODDBLESSAI.md** на версию **v3.8.2**:

***

### 1️⃣ **Таблица ksk_figurant — Добавить 1 колонка + изменить логику is_bypass**

**Где менять:** Раздел "Доменные таблицы → ksk_figurant → Структура полей"

**Было:** 15 полей

**Станет:** 16 полей — добавить в конец:


| № | Поле | Тип | Default | Наименование | Описание |
| :-- | :-- | :-- | :-- | :-- | :-- |
| 16 | bypass_name | TEXT | NULL | Имя исключения | Значение из JSON `figurant.bypassName`. NULL если отсутствует или пусто. Используется для определения is_bypass. NEW v4.2 - 17.11.2025 |

**Обновить логику поля is_bypass (поле № 9):**

Было:

```
Статус объекта в списке исключений. 
Значения: yes/no
Default: 'no' (всегда только это значение)
```

Станет:

```
Статус фигуранта относительно списка исключений.
Вычисляется на основе НАЛИЧИЯ поля bypassName в JSON figurant.
Логика:
  - if (bypassName поле есть в JSON) → 'yes' (исключён из контроля)
  - if (bypassName отсутствует в JSON) → 'no' (обычный фигурант)
Значения: yes / no
UPD v4.3 - 17.11.2025
```


***

### 2️⃣ **Глоссарий терминов — Добавить 3 определения**

**Добавить новые термины:**


| Термин | Определение |
| :-- | :-- |
| **has_bypass (ksk_result)** | Признак наличия обхода проверки на уровне записи результата. Вычисляется на основе наличия поля bypassName хотя бы у одного фигуранта. Значения: empty (нет фигурантов), yes (есть фигуранты с bypassName), no (есть фигуранты, но БЕЗ bypassName). NEW v4.1 - 17.11.2025 |
| **bypass_name (ksk_figurant)** | Имя исключения из списка исключений в JSON. Извлекается из JSON `figurant.bypassName`. NULL если поле в JSON отсутствует или пусто. Используется для определения is_bypass. NEW v4.2 - 17.11.2025 |
| **is_bypass (ksk_figurant)** | Статус фигуранта относительно списка исключений. Вычисляется на основе НАЛИЧИЯ поля bypassName в JSON: yes = поле есть (исключён), no = поля нет (обычный). ОБНОВЛЕНО v4.3 - 17.11.2025 |


***

### 3️⃣ **Раздел "Противоречия и исправления" — Добавить блок 3.8.2**

**Добавить в конец документа:**

```
## ⚠️ ОБНОВЛЕНИЯ ВЕРСИИ 3.8.2 (17.11.2025)

### Изменение 1: Таблица ksk_figurant
- **Было:** 15 колонок
- **Стало:** 16 колонок (+1 bypass_name)
- **Добавлено:** Колонка bypass_name TEXT NULL
- **Изменено:** Логика поля is_bypass
  
  БЫЛО: is_bypass всегда = 'no'
  
  СТАЛО: is_bypass вычисляется на основе НАЛИЧИЯ поля bypassName в JSON:
    - if (bypassName есть в JSON) → 'yes'
    - if (bypassName отсутствует в JSON) → 'no'

### Изменение 2: Функция put_ksk_result
- **Было:** Версия 4.2
- **Стало:** Версия 4.3
- **Изменение:** Вычисление is_bypass изменено
  
  БЫЛО: INSERT с жёстким значением 'no'
  
  СТАЛО: INSERT с динамическим вычислением:
    CASE WHEN (bypassName поле присутствует в JSON) THEN 'yes' ELSE 'no' END

### Изменение 3: Функция ksk_test_generate_data_messages
- **Было:** Версия 3
- **Стало:** Версия 4
- **Добавлено:** Генерация поля bypassName в JSON фигурантов
  
  Логика:
  - 30% фигурантов имеют поле bypassName в JSON
    - Из них 50% заполнены случайной строкой (1-10 символов)
    - Из них 50% имеют пустую строку или отсутствуют
  - 70% фигурантов НЕ имеют поле bypassName вообще
  - Применяется ТОЛЬКО для сценария "1 фигурант"
  - Для "2-4 фигуранта" bypassName не генерируется
```


***

## 📊 Сводная таблица изменений (ФИНАЛЬНАЯ)

| Объект | Версия | Дата | Тип | Описание изменения |
| :-- | :-- | :-- | :-- | :-- |
| ksk_figurant | 3.8.2 | 17.11.2025 | TABLE | +1 колонка bypass_name; is_bypass вычисляется по наличию поля в JSON |
| put_ksk_result | 4.3 | 17.11.2025 | FUNCTION | is_bypass вычисляется динамически по НАЛИЧИЮ bypassName в JSON |
| ksk_test_generate_data_messages | 4 | 17.11.2025 | FUNCTION | Генерирует bypassName для 30% фигурантов (50% из них заполнены) |


***

## ✅ Что менять в KSK-Schema-v3.8.1-GODDBLESSAI.md

**НОВОЕ ИМЯ:** `KSK-Schema-v3.8.2-GODDBLESSAI.md`

**РАЗДЕЛЫ ДЛЯ ОБНОВЛЕНИЯ:**

1. ✅ Заголовок: `**Версия:** 3.8.2` (было 3.8.1)
2. ✅ Дата: `**Дата:** 17.11.2025 23:40 MSK` (было 31.10.2025)
3. ✅ ИСТОРИЯ ИЗМЕНЕНИЙ: Добавить строку `2025-11-17 - Добавлено bypass_name в ksk_figurant, изменена логика is_bypass`
4. ✅ Раздел "Доменные таблицы → ksk_figurant" — добавить колонку bypass_name, обновить логику is_bypass (НАЛИЧИЕ поля в JSON)
5. ✅ Раздел "Глоссарий терминов" — добавить 3 новых термина
6. ✅ Раздел "Противоречия и исправления" — добавить блок 3.8.2

**ИТОГО:** 6 значительных обновлений

***

**КЛЮЧЕВЫЕ МОМЕНТЫ:**

- ❌ **БЕЗ агрегирования** (f_total, f_exclusions, f_bypassed)
- ❌ **БЕЗ v_bypass_name** — это только переменная в коде, в документацию не попадает
- ✅ **ФОКУС:** is_bypass вычисляется по **НАЛИЧИЮ поля bypassName в JSON фигуранта**
- ✅ **ФОКУС:** bypass_name — это значение из JSON (или NULL)

